pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
              echo 'Compiling the code'  
            }
        }
        stage('unitTest') {
            steps {
              echo 'Compiling and run and test cases the code'  
            }
        }
        stage('package') {
            steps {
              echo 'package the code'  
            }
        }
    }
}
